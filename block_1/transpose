#!/bin/bash

tempFile="tempfile"

# Make the input row file
numOfLines=$(wc -l < $1)
totalNum=$(wc -w < $1)
numOfRows=$(( totalNum / numOfLines ))
index=1
j=1

cat $1 | paste -s -d '\t' $1 > $tempFile

read -r string< $tempFile


cat $tempFile
rm $tempFile

